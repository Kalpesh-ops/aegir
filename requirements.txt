# Web framework + server
fastapi==0.128.0
uvicorn==0.46.0

# Auth (Supabase JWT verification) + secrets encryption-at-rest
pyjwt>=2.12.1
cryptography>=47.0.0

# Data layer
supabase==2.29.0

# Scanning engines
python-nmap>=0.7.1
scapy==2.7.0
psutil>=5.9.0          # network-interface discovery for TShark capture
defusedxml>=0.7.1      # hardened Nmap XML parsing (XXE-safe)

# AI analysis
google-generativeai==0.8.6

# HTTP client (CIRCL CVE lookups) + env loading
requests>=2.33.1
python-dotenv>=1.2.2
